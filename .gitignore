# ==========================================================
# 【最高优先级】绝对禁止提交任何 API Key / 令牌 / 密钥
# ==========================================================
# 三道防线：
#   1. 本文件（.gitignore）—— 按文件名挡住密钥类文件
#   2. hooks/pre-commit    —— 按内容挡住藏在普通文件里的密钥
#   3. scripts/scan-secrets.mjs —— 可随时手动全仓扫描
#
# 任何人不得用 `git add -f` 绕过以上规则。
# 需要在仓库内提供配置模板时，一律用 *.example.json 且值写占位符。

# ----------------------------------------------------------
# 第一道：密钥/凭据类文件（按精确文件名或扩展名，不按关键词扫）
# ----------------------------------------------------------
# 环境变量文件
**/.env
**/.env.*
!**/.env.example
!**/.env.sample
!**/.env.template

# 私钥与证书
**/*.key
**/*.pem
**/*.p12
**/*.pfx
**/*.jks
**/*.keystore

# 常见凭据文件（精确名，避免误伤正常文件）
**/credentials.json
**/secrets.json
**/service-account*.json
**/auth.json
**/token.json
**/apikey.json
**/api-key.json
**/api_key.json
**/keys.json

# 名字里明确带凭据关键词的配置（限定常见扩展名，避免误伤脚本/文档）
**/*apikey*.json
**/*api-key*.json
**/*api_key*.json
**/*.credentials.json
**/*.secret.json
**/*-key.json
**/*_key.json
**/*.key.json
**/*.token.json
**/*-token.json
**/*-secrets.json
!**/*.example.json
!**/*.sample.json
!**/*.template.json

# ----------------------------------------------------------
# 第二道：本机实跑产物与临时取证材料（常含真实密钥）
# ----------------------------------------------------------
*.local.json
*.local.*
data/
*.db
*.db-wal
*.db-shm
*.bak
*.bak-*
_*.mjs
tmp/
.tmp/
**/*.request-body.json
**/*-captured*.json

# ----------------------------------------------------------
# 依赖与缓存
# ----------------------------------------------------------
node_modules/
__pycache__/
*.pyc

# ----------------------------------------------------------
# 编辑器与系统
# ----------------------------------------------------------
.vscode/
.idea/
.DS_Store
Thumbs.db
